apiVersion: v2
name: kubecost
description: A Helm chart for kubecost

type: application

version: 0.1.0

appVersion: "2.7.0"

dependencies:
  - name: cost-analyzer
    repository: https://kubecost.github.io/cost-analyzer
    version: 2.9.3
