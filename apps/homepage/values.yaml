image:
  repository: ghcr.io/gethomepage/homepage
  tag: "latest@sha256:643bd0be730d40f69d58028a55d1a896739333e8815786df42bc97f109ecbe61"
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  host: homepage.shivpatel.xyz

allowedHosts: "shivpatel.xyz,homepage.shivpatel.xyz"

externalSecret:
  enabled: true
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  vaultPath: secret/data/homepage
