apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik-ingress
  namespace: authentik
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`authentik.shivpatel.xyz`)
    kind: Rule
    services:
    - name: authentik
      port: 30080
  tls: {}