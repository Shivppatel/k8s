apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard-ingress
  namespace: traefik
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`traefik.shivpatel.xyz`)
    kind: Rule
    services:
    - name: traefik-dashboard
      port: 8080
  tls: {}