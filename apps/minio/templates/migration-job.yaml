# apiVersion: batch/v1
# kind: Job
# metadata:
#   name: minio-data-migration
#   namespace: minio
# spec:
#   template:
#     spec:
#       containers:
#       - name: rsync
#         image: instrumentisto/rsync-ssh
#         command: ["rsync", "-avp", "/mnt/old/", "/mnt/new/"]
#         volumeMounts:
#         - name: old-vol
#           mountPath: /mnt/old
#         - name: new-vol
#           mountPath: /mnt/new
#       restartPolicy: Never
#       volumes:
#       - name: old-vol
#         persistentVolumeClaim:
#           claimName: minio
#       - name: new-vol
#         persistentVolumeClaim:
#           claimName: minio-nfs
#   backoffLimit: 1
