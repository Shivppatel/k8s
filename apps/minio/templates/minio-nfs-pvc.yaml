apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-nfs
  namespace: minio
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 500Gi
  volumeName: minio-nfs
