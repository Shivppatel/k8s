# apiVersion: batch/v1
# kind: Job
# metadata:
#   name: nextcloud-data-migration
#   namespace: nextcloud
# spec:
#   template:
#     spec:
#       containers:
#       - name: rsync
#         image: instrumentisto/rsync-ssh
#         command: ["rsync", "-avp", "/mnt/old/", "/mnt/new/"]
#         volumeMounts:
#         - name: old-vol
#           mountPath: /mnt/old
#         - name: new-vol
#           mountPath: /mnt/new
#       restartPolicy: Never
#       volumes:
#       - name: old-vol
#         persistentVolumeClaim:
#           claimName: nextcloud-nextcloud
#       - name: new-vol
#         persistentVolumeClaim:
#           claimName: nextcloud-nfs
#   backoffLimit: 1
