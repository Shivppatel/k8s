apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana-ingress
  namespace: observability
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`grafana.shivpatel.xyz`)
    kind: Rule
    services:
    - name: grafana
      port: 3000
  tls: {}