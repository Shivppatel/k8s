# App role credentials are pulled from Vault into Kubernetes and reconciled by
# CNPG. The reserved postgres superuser secret is generated by CNPG and pushed
# back to Vault so the operator stays the single owner of that password.
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: postgresql-superuser
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  deletionPolicy: None
  refreshInterval: "1h"
  updatePolicy: Replace
  secretStoreRefs:
    - name: vault-backend
      kind: ClusterSecretStore
  selector:
    secret:
      name: postgresql-superuser
  data:
    - conversionStrategy: None
      match:
        secretKey: username
        remoteRef:
          remoteKey: postgresql/superuser
          property: username
    - conversionStrategy: None
      match:
        secretKey: password
        remoteRef:
          remoteKey: postgresql/superuser
          property: password
---
# Secret for CNPG managed role - immich
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: immich-db-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: immich-db-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/postgresql/immich
        nullBytePolicy: Ignore
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/postgresql/immich
        nullBytePolicy: Ignore
        property: password
---
# Secret for CNPG managed role - n8n
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: n8n-db-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: n8n-db-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/postgresql/n8n
        nullBytePolicy: Ignore
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/postgresql/n8n
        nullBytePolicy: Ignore
        property: password
---
# Secret for CNPG managed role - terraform
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: terraform-db-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: terraform-db-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/postgresql/terraform
        nullBytePolicy: Ignore
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/postgresql/terraform
        nullBytePolicy: Ignore
        property: password
---
# Secret for CNPG managed role - teslamate
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: teslamate-db-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: teslamate-db-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/postgresql/teslamate
        nullBytePolicy: Ignore
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/postgresql/teslamate
        nullBytePolicy: Ignore
        property: password
---
# Secret for CNPG managed role - authentik
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: authentik-db-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: authentik-db-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/postgresql/authentik
        nullBytePolicy: Ignore
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/postgresql/authentik
        nullBytePolicy: Ignore
        property: password
---
# Secret for CNPG managed role - gitlab
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-db-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: gitlab-db-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/postgresql/gitlab
        nullBytePolicy: Ignore
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/postgresql/gitlab
        nullBytePolicy: Ignore
        property: password
---
# Secret for CNPG managed role - datadog
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: datadog-db-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: datadog-db-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/postgresql/datadog
        nullBytePolicy: Ignore
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/postgresql/datadog
        nullBytePolicy: Ignore
        property: password
---
# Secret for CNPG managed role - lnd
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lnd-db-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: lnd-db-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/postgresql/lnd
        nullBytePolicy: Ignore
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/postgresql/lnd
        nullBytePolicy: Ignore
        property: password
