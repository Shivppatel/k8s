apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgresql
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  instances: 1
  
  # Note: For pgvector support, you may need a custom image
  # Options: ghcr.io/tensorchord/cloudnative-pgvecto.rs:17-v0.4.0 (uses 'vectors' extension)
  # Or build custom image with pgvector
  imageName: ghcr.io/cloudnative-pg/postgresql:17
  
  # Use superuser credentials from Vault
  superuserSecret:
    name: postgresql-superuser
  
  # Bootstrap - create databases and extensions
  bootstrap:
    initdb:
      database: postgres
      owner: postgres
      postInitSQL:
        - CREATE DATABASE immich
        - CREATE DATABASE keycloak

  # Managed roles - CNPG handles user creation/password sync
  managed:
    roles:
      - name: immich
        login: true
        passwordSecret:
          name: immich-db-secret
      - name: keycloak
        login: true
        passwordSecret:
          name: keycloak-db-secret

  storage:
    size: 10Gi
